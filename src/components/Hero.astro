---
---

<div class="hero">
  <div class="wrapper">
    <h1>Bring Your Stories to Life with <span>ArcAide</span></h1>
    <p>
      A revolutionary tool designed for world builders, dungeon masters,
      authors, and creatives of every kind.
    </p>
    <a href="#" class="cta-button">Start Now for Free</a>
  </div>
</div>

<style>
  .hero {
    height: 100vh;
    display: grid;
    place-items: center;
    text-align: center;
    background-image: url("fantasy_world.png");
    background-size: cover;
    color: #fff;
    margin-bottom: 3rem;
  }
  h1 {
    color: #000;
    text-shadow: 0 0 20px rgba(255, 255, 255, 0.5);
    margin: 0;
  }
  h1 span {
    color: var(--clr-primary);
  }
  p {
    text-shadow: 0 0 20px rgba(0, 0, 0, 0.5);
    font-size: var(--fs-700);
    max-width: 30ch;
    margin-top: 0;
    margin-bottom: 2rem;
    margin-inline: auto;
  }
  a.cta-button {
    background-color: var(--clr-accent);
    padding: 1rem 2rem;
    color: white;
    text-decoration: none;
    font-weight: 500;
    border-radius: 100vw;
  }
  a.cta-button:hover {
    color: var(--clr-secondary);
  }
</style>
